'use strict';

module.exports = exports = {};


/**
  * @param  {} arr gets sorted array
  * @param  {} searchKey gets search key
  * @returns return the index of the array’s element that is equal to the search key, or -1 if the element does not exist
 */

exports.aBinarySearch = (arr, searchKey) => {
  let index;



  return index; 
};